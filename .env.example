# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# The only required variable. The runner reads it via the Anthropic SDK.
ANTHROPIC_API_KEY=

# Optional. Defaults to claude-opus-5 when unset.
AGENT_EVALS_MODEL=claude-opus-5